description = "A Simplest Bean to Bean mapper Library"

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.12'
}

apply from: "${rootProject.projectDir}/gradle/publish.gradle"
